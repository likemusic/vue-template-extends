Domain contexts:
 - Base
 - Child
 - Grandchild

Feature contexts:
 - Without overrides
 - With overrides
   - By Slot name
     - default
     - custom
   - By replacement override type (replace attribute):
     - (slot) content (only)
     - (full) slot 
   - By 'parent' usage:
     - no `parent`;
     - has `parent`;
